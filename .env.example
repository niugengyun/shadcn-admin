# Backend API origin. Keep /api when using the Vite proxy.
VITE_API_BASE_URL=/api
# Request timeout in milliseconds.
VITE_API_TIMEOUT=30000
# Set true when the backend exposes mock-compatible endpoints.
VITE_USE_MOCK=true
