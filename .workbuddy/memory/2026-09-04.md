2026-09-04：从空目录初始化 Vite + React + TypeScript，完成 AGENTGATEWAY 控制平面首页。实现无 Header、扁平侧栏、Overview 指标卡、协议分布、策略概览、快捷操作、健康状态横幅与 next-themes 黑白切换；生产构建已通过。
2026-09-04：扩展中英双语字典与 localStorage 记忆，中文默认；移除重启节点按钮；新增底部语言切换与全宽内容布局；新增基于 Recharts 的 Token 与成本监控 Metrics 页面，含 KPI、趋势图、模型分布和请求明细表；构建通过。
2026-09-04：控制台收敛为 Overview、智能体网关、LLM 网关、Token 统计四个一级入口；新增 Agents 实例管理与终端日志侧滑层、Routes 上游节点开关/编辑弹窗/故障转移可视化；Overview 改为三大业务模块联动指标和快捷入口；Metrics 迁移至 src/pages/Metrics.tsx；生产构建通过。
2026-09-04：将页面统一整理到 src/pages，新增 src/services/http.ts 统一 fetch 封装和 .env.example，并重写 README.md，覆盖启动、构建、目录、i18n、主题、页面用法、Mock/真实 API 接入和扩展规范；build 通过，lint 0 errors 但保留 i18n Fast Refresh warning。
2026-09-04：初始化本地 Git 仓库，创建提交 747bb2a，并推送至 git@github.com:niugengyun/shadcn-admin.git 的 origin/main；工作区干净。
2026-09-04：根据 satnaing/shadcn-admin 参考模式完成侧栏改造：桌面展开/图标折叠、移动端抽屉、localStorage 持久化；语言控件压缩为图标+中/EN 单行，主题切换改为独立图标状态，菜单移除右侧数字。README 增补响应式设计、目录分层和测试建议；构建通过并推送提交 a828fd1。
2026-09-04：为左侧四个一级菜单接入 History API 路由：/、/agents、/routes、/metrics；菜单改为语义化链接，支持直接访问、未知路径回退首页、浏览器前进后退，生产构建通过。
2026-09-04：将侧栏左下语言与主题操作区重构为两个对称的纯图标按钮，统一 32px 方形尺寸、Zinc 黑白边框/悬停样式和悬浮提示；语言按钮直接中英 Toggle，保留版本号与运行状态，生产构建通过。
2026-09-04：仅重构 src/pages/Metrics.tsx 与直接相关 i18n 词条，补齐 Token 审计控制栏、四项指标、堆叠面积趋势、Agent/Model 排行、可筛选调用明细与中英文案；清理越界的 App.css/App.tsx 改动，生产构建通过。
